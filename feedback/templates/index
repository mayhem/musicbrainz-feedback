{% extends 'layout' %}
{% block body %}
<div class="hero-unit">
<h1>Poot!</h1>
<p>Logged in user:
{% if current_user.get_id() %}
    {{ current_user.get_id() }}. user object exists.</p>
{% else %}
    <p>user not logged in.</p>
{% endif %}
<div>
{% endblock %}
